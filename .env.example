# Linear MCP Authentication Configuration

# Option 1: Personal Access Token (PAT) Authentication (Recommended)
# Ideal for organizations where you don't have admin access
# Generate a PAT at https://linear.app/settings/api
LINEAR_PAT=your_personal_access_token

# Option 2: OAuth Authentication
# For organizations where you have admin access
# Create an OAuth application at https://linear.app/settings/api/applications
# LINEAR_CLIENT_ID=your_oauth_client_id
# LINEAR_CLIENT_SECRET=your_oauth_client_secret
# LINEAR_REDIRECT_URI=http://localhost:3000/callback

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: For testing and development only
# LINEAR_AUTH_CODE=code_from_oauth_redirect
# LINEAR_REFRESH_TOKEN=refresh_token_from_oauth_flow
